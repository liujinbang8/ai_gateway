# 📘 LLM Gateway 产品功能说明书（V1.1）

版本：1.1  
状态：设计评审版  
定位：公司内部统一大模型接入与治理平台

---

# 一、产品背景与建设目标

## 1.1 背景

随着大模型在公司内部全面落地，不同业务系统分别接入：

- OpenAI
- Anthropic（Claude）
- Gemini
- 阿里云
- GLM

当前主要问题：

- 多厂商接口不统一，集成成本高
- 权限控制分散，缺乏统一治理
- Token 与成本无法精确归因
- 缺乏统一安全治理（敏感词、黑白名单、注入防护）
- 供应商不稳定时缺乏统一降级策略
- 运维可观测能力不成体系

---

## 1.2 建设目标

构建一个统一、可治理、安全合规、可观测的大模型网关平台。

V1.1 目标（对应 7 大能力）：

1. 模型管理（可视化接入多厂商，多模型类型）
2. 用户权限管理（RBAC + 模型级授权）
3. 审计与管理（全链路日志与管理审计）
4. AD 域鉴权（OIDC 接入）
5. 可观测运维（SLO/告警/指标/Tracing）
6. 安全治理（敏感词、提示词注入检测、黑白名单、审计留痕）
7. 计量计费（用户-模型、组织-模型，周/月/季度报表）

---

# 二、产品定位

LLM Gateway 是公司内部「AI 能力统一接入与治理中枢」。

明确边界：

- 不是模型训练平台
- 不是知识库系统
- 不是业务应用系统
- 是所有业务调用大模型的统一出口

---

# 三、系统整体架构

系统分为三层：

## 3.1 数据面（Data Plane）

负责：

- OpenAI/Anthropic 兼容入口
- API Key 鉴权
- 模型授权校验
- 安全治理拦截（敏感词/注入/黑白名单）
- 路由决策与 fallback
- 厂商调用
- 计量与成本落账
- ES 日志写入

## 3.2 控制面（Control Plane）

负责：

- 模型管理
- 用户/角色/权限管理
- API Key 管理
- 路由策略
- 配额与成本报表
- 安全治理策略管理
- 管理审计

## 3.3 可观测与治理层

负责：

- 指标采集（Prometheus）
- 日志检索（Elasticsearch）
- 链路追踪（Trace）
- SLO 看板与告警
- 异步任务运行态监控

---

# 四、核心功能模块

## 4.1 模型管理

支持：

- 多厂商模型录入
- 模型类型：CHAT / EMBEDDING / RERANK / VL / OCR
- 启用/禁用
- 定价维护（input/output）
- 每模型独立凭据
- 超时配置

---

## 4.2 协议兼容与接口契约

### OpenAI 协议（`/openai/v1`）

同步接口：
- `POST /chat/completions`（CHAT）
- `POST /embeddings`（EMBEDDING）
- `POST /rerank`（RERANK）
- `POST /responses`（VL）
- `POST /ocr`（OCR）

异步接口：
- `POST /jobs`
- `GET /jobs/{job_id}`

### Anthropic 协议（`/claude/v1`）

同步接口：
- `POST /messages`（CHAT/VL）
- `POST /embeddings`（EMBEDDING，网关扩展）
- `POST /rerank`（RERANK，网关扩展）
- `POST /ocr`（OCR，网关扩展）

异步接口：
- `POST /jobs`
- `GET /jobs/{job_id}`

统一异步 `job_type`：
- CHAT / EMBEDDING / RERANK / VL / OCR / EMBEDDING_BATCH

---

## 4.3 API Key 管理

支持：

- 个人 Key / 应用 Key
- Key 绑定组织（部门）
- Key 模型收敛
- 过期/吊销

安全规则：

- 明文仅展示一次
- 数据库存储 hash
- 不可逆

---

## 4.4 权限管理（RBAC + 模型级授权）

支持：

- 用户/应用主体
- 角色
- 菜单权限
- 按钮权限
- 模型权限（精确到模型）

有效模型计算：

`角色模型集合 ∩ Key 收敛模型集合`

---

## 4.5 路由策略

支持：

- 成本优先
- 质量优先
- 延迟优先
- 部门维度配置
- 失败 fallback
- fallback 轨迹记录

---

## 4.6 计量与计费

统计维度最小粒度：

- 用户-模型（user-model）
- 组织-模型（dept-model）

统计周期：

- 周
- 月
- 季度

支持报表：

- 用量报表（tokens/请求/成功率）
- 成本报表（cost）
- 导出（CSV）

费用计算：

`input_tokens × input_price + output_tokens × output_price`

---

## 4.7 配额管理

支持：

- 用户月度 token cap
- 组织月度 token cap
- 超额拒绝（429）

---

## 4.8 异步任务

适用场景：

- OCR 大文件
- VL 多页图文
- 批量 Embedding/Rerank

实现：

- Redis Stream 队列
- Worker 消费
- 状态机（PENDING → RUNNING → SUCCEEDED/FAILED/CANCELED）
- 重试机制

---

## 4.9 文件直传与存储

支持：

- Multipart 上传
- MinIO 存储
- MySQL 元数据
- file_id 全链路可追溯

---

## 4.10 日志与审计

ES 元数据必存：

- request_id
- subject/dept/model
- tokens/cost/latency
- status/error_code

原文日志：

- 默认关闭
- 可开启
- 开启前后均需审计
- 写入前必须脱敏

管理审计：

- 模型、凭据、路由、权限、Key、安全策略等高风险操作全记录

---

## 4.11 OIDC 与 AD 集成（不做组织同步）

支持：

- OIDC 登录
- 用户自动创建/更新
- 默认角色分配

明确不做（V1.1）：

- AD 组织/群组自动同步
- groups claim 到角色自动同步

组织与角色维护方式：

- 由管理员在后台手工维护（用户-部门、用户/应用-角色）

---

## 4.12 安全治理（V1.1 强化）

覆盖能力：

- 敏感词管理（GLOBAL/DEPT/USER 作用域）
- 用户/组织白名单豁免
- 黑白名单（用户/组织/应用/IP）
- 提示词注入检测（OFF/DETECT/BLOCK）
- 内容安全策略（风险分级与动作）
- 安全事件记录与检索
- 安全报表（周/月/季度）

动作：

- ALLOW / WARN / BLOCK

---

## 4.13 可观测运维（V1.1 强化）

交付内容：

- SLO 目标（可用性、延迟、异步成功率、落账及时性）
- 告警规则（P1/P2/P3）
- 指标字典（Prometheus 指标命名）
- Tracing 规范（x-request-id + traceparent + span 规范）
- 运维处置建议（Runbook）

---

# 五、非功能目标

## 5.1 性能

- 峰值 QPS：100（可按环境扩展）
- WebFlux 支持流式
- 异步解耦长任务

## 5.2 安全

- API Key Hash 存储
- 厂商凭据加密
- 不存明文密钥
- 日志脱敏
- 安全事件可审计

## 5.3 可用性

- 管理面与数据面安全链路隔离
- ES 故障不影响主链路返回（best effort）

---

# 六、典型流程

## 6.1 业务系统调用

1. 调用 OpenAI/Anthropic 兼容接口
2. 网关鉴权与授权
3. 安全治理检测（敏感词/注入/黑白名单）
4. 路由并调用厂商
5. 记录 usage/cost
6. 写 ES 与审计
7. 返回结果

## 6.2 管理员配置

1. OIDC 登录后台
2. 配置模型与价格
3. 配置角色模型权限
4. 发放 API Key
5. 配置安全治理策略
6. 查看成本与安全报表

---

# 七、版本边界（V1.1 不做）

- 灰度流量百分比分流
- 动态限流
- 多租户隔离
- 自动成本优化推荐
- 模型自动评估
- AD 组织自动同步

---

# 八、未来演进（V2+）

- 自动策略优化
- 自适应风控策略
- 多租户与多 Region 调度
- RAG 与向量索引统一入口
- 安全策略智能推荐

---

# 九、价值总结

LLM Gateway 的核心价值：

- 接入标准化
- 权限集中化
- 成本透明化
- 安全可治理
- 运维可观测
- 审计可追溯
- 平台可扩展

